import loading from '../assets/loading2.svg';

const Loading = () => {
  return (
    <img 
      src={loading}
      alt="loading..." 
      style={{
        width: '20px',
        height: '20px'
      }}
    />
  )
}

export default Loading